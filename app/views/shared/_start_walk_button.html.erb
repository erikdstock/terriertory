<div class='row'>
  <div class='small-9 small-centered columns'>
  <% if session[:walk_id] %>
    <a id="continue-walk-button" class = "button small expand radius warning " href="<%= walk_path(session[:walk_id]) %>/#live-walk"> Finish Your Walk!</a>
  <% else %>
      <form action = "/walks" method = "post">
        <% if @user.dogs %>
          <% @user.dogs.each do |dog| %>
            <input type = "hidden" name="dogs[<%=dog.id%>]" value='<%=dog.id%>'></input>
            <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
          <% end %>
        <% end %>
            <input class = "button small expand radius success " type = "submit" value = "Take A Walk!"></input>
        </div>
      </form>
  <% end %>
  </div>
</div>
